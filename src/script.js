setUpPrice(document.querySelector('.price'))

function setUpPrice(serviceSelection){
  const service = serviceSelection.querySelectorAll('.service')
  const buttons = serviceSelection.querySelectorAll('.service-selection_btn')
}